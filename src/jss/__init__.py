"""jss: judge sensitivity & steerability.

A small, standalone framework (inspired by "Safety is Contextual, LLM-Judges Are
Not") to measure how *sensitive* and *steerable* an arbitrary LLM safety judge
is. Pure Python with no heavy framework dependencies.
"""

__version__ = "0.1.0"
